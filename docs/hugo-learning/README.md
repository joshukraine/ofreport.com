# Hugo Learning Notes

Line-by-line walkthroughs of Hugo templates in this project, explaining Go
template syntax and Hugo-specific concepts as they appear in real code.

Each file documents a specific template at a point in time. The YAML metadata
at the top of each file records the template path, date, and commit hash so
you can compare against the current version if the template has changed since
the explanation was written.
